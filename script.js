var petName = prompt("Name your pet.");

var answer = ""

//user enters name and the next prompt starts the reactions

answer = prompt ["feed, pet, or walk?"]; // at the end, after the six actions, log the pet's name, happiness, and energy level to the console. (console log)
let energyTotal = 0;
let happinessToal = 0;
for (let i = 0; i < 2; i++) {
    let feed = prompt ("feed, pet, or walk?");
    let pet = prompt ("feed, pet, or walk?");
    let walk = prompt ("feed, pet, or walk?");
}



//now need a function for feed, pet, and walk.

if (total <= 0) {
    alert("Not enough energy to enjoy a walk.")
} else {

}

